CONFIG_ZMK_KEYBOARD_NAME="Tux"

# BLE disabled - enable after verifying basic matrix function
CONFIG_ZMK_BLE=n

# Sleep disabled until wakeup source is configured
CONFIG_ZMK_SLEEP=n

# Mouse emulation
CONFIG_ZMK_POINTING=y
